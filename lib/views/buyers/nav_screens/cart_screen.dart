import 'package:flutter/material.dart';

class CartScreeen extends StatelessWidget {
  const CartScreeen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('CartScreeen'),);
  }
}
